[build]
buildCommand = "cd backend && npm install && npx prisma generate --schema=../prisma/schema.prisma && npx nest build"

[deploy]
startCommand = "cd backend && node dist/main"
restartPolicyType = "on_failure"
