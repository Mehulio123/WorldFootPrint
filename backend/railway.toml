[build]
buildCommand = "npm install && npx prisma generate --schema=../prisma/schema.prisma && npx nest build"

[deploy]
startCommand = "node dist/main"
healthcheckPath = "/"
restartPolicyType = "on_failure"
